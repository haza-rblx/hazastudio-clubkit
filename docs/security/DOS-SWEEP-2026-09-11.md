# DoS / server-crash sweep — 2026-09-11

Scope: what a malicious client can do to crash, stall or starve a server by firing remotes or abusing
replication. Complements `AUDIT-2026-08-29.md` (authorization + budget); items listed there are not repeated.
Method: one read-only pass over all 66 `OnServerEvent` handlers (no `OnServerInvoke`, no `InvokeClient`
exists), follow-through into services for ~40. D1 re-verified by hand.

## Still open from the 2026-08-29 audit (crash/budget relevant)

- **H2** `ProfileMenuController.luau:97-118` — no limiter / size check.
- **H3** `DonationService.luau:726-740` — `DonationNotifDisplay` takes any `donationId`, no limiter, HTTP flush per call.
- **H4** `SettingsController.luau:309` — `SettingsReset` no limiter, 2 DataStore writes per click.
- **H1** `STUDIO_GIFT_BYPASS = IS_STUDIO` still set (not DoS, but still open).
- **M8, M9, M13** unchanged.

## New findings

| ID | Where | Class | Exploiter sends | Effect | Confidence |
|---|---|---|---|---|---|
| D1 | `MusicController.luau:423-433` → `MusicService.luau:680-717` → `MusicSession.luau:922-960` | unvalidated number | `DurationReport {trackId=<current>, totalDuration=0.001}` — **any player**, 4×/10 s | `newExpectedEnd <= now+5` → `advanceQueue()`: skips the track for the whole server, no vote. `math.huge` → expected end `inf`, track never ends / vote never reaches quorum, and `inf` is written to the library via `updateTrackDuration`. | skip: **verified**; stuck + library write: likely |
| D2 | `OverheadController.luau:1550-1561`, `GravityController.luau:138/141`, `DroneController.luau:61/64`, `CinematicDockController.luau:298/315` | work before gate | spam | `buildPayloadWithMeta` (repo load + BindableEvent + ≥3 `task.spawn`) runs **before** the limiter/admin check; Cinematic has no limiter. CPU/thread churn; cold cache = real GetAsync per call. | ordering certain, magnitude likely |
| D3 | `Validator.checkSize` before limiter in Shop, Couple, Admin, AvatarContext, AvatarLike, Donation, PaidBroadcast, Overhead, Carry controllers | work before gate | large nested table per fire | full `JSONEncode` of client payload before the per-player gate. Sticker/Music do limiter first (correct). | order certain, impact bounded by engine network throttle |
| D4 | `StickerController.luau:154-172` | amplification | `StickerAdd`, 5×/10 s | 1 `UpdateAsync` (+ global pool write) and a sync fired to **every** player per call; N spammers → N² fires/s, shared DataStore budget starves. | certain / likely |
| D5 | `GravityController.luau:168-174` → `GravityService.luau:74-80,398,417,483-487` | NaN | `{action="set", speed=0/0}` | NaN passes `type=="number"` and `<= 0`; `LinearVelocity.VectorVelocity = (0,NaN,0)`, `MaxForce = inf` on every HRP each Heartbeat. **Admin-gated.** | path certain, engine outcome likely |
| D6 | `DroneController.luau:142-156` → `DroneService.luau:77-108` | NaN | pilot streams NaN CFrame components | `FireAllClients` NaN CFrame → every viewer's camera breaks. **Pilot/admin-gated.** | likely |
| D7 | Shop, Overhead, Gravity, Carry, Admin, AvatarLike controllers | cache keyed by raw `requestId` | random ids at limiter rate | M13 pattern in files M13 missed: O(n) sweep + cross-player replay. Memory bounded. | certain, low |
| D8 | `SettingsController.luau:278-280`, `ProfileMenuController.luau:93-95` | no limiter | spam `request` | cached loads, full payload echoed back to the caller only. | certain, low |

Checked and clean: Lua pattern injection (fixed patterns or `plain=true` everywhere), `string.rep`/format width
from client, recursive walks over client tables (only the depth-capped `validateStyleDepth`).

## Global backstops (config as of this date)

- `RuntimeGuard` RemoteStorm: counts every RemoteEvent per player, 40/s over 3 s — **`REMOTE_STORM_ACTION = "log"`** (nobody is removed).
- `MovementGuard.ENFORCE = "log"` — the one-week review was due 2026-09-06.
- `SOUND_ENFORCE = "log"`, `SCRIPT_GUARD = "beacon"`, `AvatarGuard.EFFECTS = "cap"` (active).

## Solid (keep)

`RateLimiter` token bucket on ~55/66 handlers; `Validator.checkSize` byte caps; `TTLSet` dedupe; `TaskQueue`
pcall-wrapped serial queues; repositories behind `BudgetGate` + negative cache + throttle lockout; numeric
clamps in Sync speed, DJ speed, drone FOV, carry amount.
