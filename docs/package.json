{
  "name": "hazastudio-clubkit-docs",
  "version": "1.0.0",
  "private": true,
  "description": "Static docs site for Hazastudio Club Kit — deployed as a Cloudflare Workers static-assets site.",
  "scripts": {
    "cf:dev": "wrangler dev",
    "cf:deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.23.0"
  }
}
